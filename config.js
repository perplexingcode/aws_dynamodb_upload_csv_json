const tableName = "verifi";
const uuid_key = "id";
//Use field_name to create a column with all rows set to a default value, if there will be no such column, set this to empty: const field_name = "";
const field_name = "";

module.exports = { tableName, uuid_key, field_name };
